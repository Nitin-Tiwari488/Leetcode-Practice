class Solution {
    public boolean uniformArray(int[] nums1) {
        // Only the observation in this question is when even and odd
        // both numbers are present we can change them only odd number using parity like..
        // If you substract any even number by odd number you will always find odd number 
        // so nums1 can be converted to nums2 
        return true;
    }
}